
  <h6 class="page-title mb-4  mt-3 mx-auto bg-secondary border border-primary rounded text-light text-truncate">
    <b class="mx-2">ログイン</b>
  </h6>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email, "メールアドレス" %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password, "パスワード" %>
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, "次回からログイン状態を維持する" %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン", class: "btn btn-primary" %>
  </div>
<% end %>

<p>登録がお済みでない方はこちらから <%= render "public/shared/links" %></p>
<p>お試しの際はこちらから <%= link_to "ゲストログイン" , end_user_guest_sign_in_path, method: :post %></p>
